{
  "name": "idv-project",
  "version": "1.0.0",
  "description": "individual project source code",
  "repository": "https://github.com/tedigc/individual-project",
  "main": "server/src/server.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && babel-node src/server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "deploy": "npm run build && git push heroku master --force"
  },
  "author": "tedigc",
  "license": "ISC",
  "dependencies": {
    "pg": "^6.1.0",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-redux": "^5.0.1",
    "redux": "^3.6.0",
    "redux-thunk": "^2.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-plugin-import": "^1.1.0",
    "concurrently": "^3.1.0",
    "react-scripts": "^0.8.4"
  }
}
